apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-rules.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-rules.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-rules.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
