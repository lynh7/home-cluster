---
# Source: alerting-stack/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: alerting-stack
  namespace: monitoring
  labels:
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
---
# Source: alerting-stack/templates/server-alerts-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: alerting-stack-server-alert-rules-config
  namespace: monitoring
  labels:
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
data:
  alert-rules.yaml: |
    
    groups:
    - name: app-critical-cnpg
      rules:
      - alert: CloudNativePGPodNotReady
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cloudnative-pg/cloudnative-pg?var-namespace={{
            $labels.namespace }}&var-instances={{ $labels.pod }}
          description: CloudNativePG pod {{ $labels.namespace }}/{{ $labels.pod }} has
            remained NotReady for more than 10 minutes.
          impact: Kyoo database availability may be degraded or unavailable.
          investigate: Check CNPG pod events, storage state, secrets, and postgres logs.
          runbook_url: https://cloudnative-pg.io/documentation/current/troubleshooting/
          summary: CNPG pod {{ $labels.namespace }}/{{ $labels.pod }} is not Ready
        expr: max by (cluster, namespace, pod) (kube_pod_status_ready{namespace="stag01",pod=~"cnpg-cluster-.*",condition="true"}
          == 0) > 0
        for: 10m
        labels:
          app: cnpg
          category: database
          scope: app
          severity: critical
      - alert: CloudNativePGConnectionsHigh
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cloudnative-pg/cloudnative-pg?var-namespace={{
            $labels.namespace }}&var-instances={{ $labels.pod }}
          description: CloudNativePG pod {{ $labels.namespace }}/{{ $labels.pod }} is
            above 85% of max PostgreSQL connections for more than 15 minutes.
          impact: Kyoo requests may fail or queue when new database sessions cannot be
            opened.
          investigate: Check active Kyoo workloads, idle sessions, and whether pooler
            or app-side connection limits need tuning.
          runbook_url: https://cloudnative-pg.io/documentation/current/monitoring/
          summary: CNPG pod {{ $labels.namespace }}/{{ $labels.pod }} is near max connections
        expr: 100 * sum by (cluster, namespace, pod) (cnpg_backends_total{namespace="stag01",pod=~"cnpg-cluster-.*"})
          / sum by (cluster, namespace, pod) (cnpg_pg_settings_setting{name="max_connections",namespace="stag01",pod=~"cnpg-cluster-.*"})
          > 85
        for: 15m
        labels:
          app: cnpg
          category: database
          scope: app
          severity: warning
      - alert: CloudNativePGMetricsCollectionError
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cloudnative-pg/cloudnative-pg?var-namespace={{
            $labels.namespace }}&var-instances={{ $labels.pod }}
          description: CloudNativePG exporter reports collection errors for pod {{ $labels.namespace
            }}/{{ $labels.pod }}.
          impact: Database health may degrade while monitoring visibility becomes incomplete.
          investigate: Check postgres logs, exporter health, and whether the instance
            is under storage or CPU pressure.
          runbook_url: https://cloudnative-pg.io/documentation/current/monitoring/
          summary: CNPG metrics collection is failing on {{ $labels.namespace }}/{{ $labels.pod
            }}
        expr: max by (cluster, namespace, pod) (cnpg_collector_last_collection_error{namespace="stag01",pod=~"cnpg-cluster-.*"})
          > 0
        for: 10m
        labels:
          app: cnpg
          category: database
          scope: app
          severity: warning
    - name: cluster-critical-health
      rules:
      - alert: KubernetesNodeNotReady
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/dUMN5x0mk/kubernetes-nodes
          description: Kubernetes node {{ $labels.node }} has reported NotReady for more
            than 10 minutes.
          impact: Pods may not schedule or may be evicted. Cluster redundancy is reduced.
          investigate: Check node status, recent events, kubelet health, Cilium state,
            and host resource pressure.
          runbook_url: https://kubernetes.io/docs/tasks/debug/debug-cluster/
          summary: Node {{ $labels.node }} is not Ready
        expr: kube_node_status_condition{condition="Ready",status="true"} == 0
        for: 10m
        labels:
          category: kubernetes
          scope: node
          severity: critical
      - alert: KubernetesDeploymentReplicasMismatch
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}&var-pod={{ $labels.deployment }}.*
          description: Available replicas for deployment {{ $labels.namespace }}/{{ $labels.deployment
            }} are below spec for more than 15 minutes.
          impact: User-facing capacity or availability may be degraded.
          investigate: Check rollout status, pod scheduling, image pulls, recent events,
            and dependent services.
          runbook_url: https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
          summary: Deployment {{ $labels.namespace }}/{{ $labels.deployment }} is below
            desired replicas
        expr: kube_deployment_spec_replicas{namespace!~"kube-system|monitoring"} > kube_deployment_status_replicas_available{namespace!~"kube-system|monitoring"}
        for: 15m
        labels:
          category: kubernetes
          scope: workload
          severity: critical
      - alert: KubernetesStatefulSetReplicasMismatch
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}&var-pod={{ $labels.statefulset }}.*
          description: Ready replicas for statefulset {{ $labels.namespace }}/{{ $labels.statefulset
            }} are below spec for more than 15 minutes.
          impact: Stateful app durability or service availability may be reduced.
          investigate: Check PVC binding, pod startup, storage backend health, and recent
            events.
          runbook_url: https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/
          summary: StatefulSet {{ $labels.namespace }}/{{ $labels.statefulset }} is below
            desired replicas
        expr: kube_statefulset_replicas{namespace!~"kube-system|monitoring"} > kube_statefulset_status_replicas_ready{namespace!~"kube-system|monitoring"}
        for: 15m
        labels:
          category: kubernetes
          scope: workload
          severity: critical
      - alert: KubernetesPersistentVolumeClaimPending
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}
          description: PersistentVolumeClaim {{ $labels.namespace }}/{{ $labels.persistentvolumeclaim
            }} has remained Pending for more than 15 minutes.
          impact: Stateful workloads may fail to start or recover.
          investigate: Check storage class, Longhorn health, capacity, and PVC events.
          runbook_url: https://kubernetes.io/docs/concepts/storage/persistent-volumes/
          summary: PVC {{ $labels.namespace }}/{{ $labels.persistentvolumeclaim }} is
            Pending
        expr: kube_persistentvolumeclaim_status_phase{phase="Pending"} == 1
        for: 15m
        labels:
          category: storage
          scope: pvc
          severity: critical
      - alert: KubernetesJobFailures
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}
          description: Job {{ $labels.namespace }}/{{ $labels.job_name }} reports one
            or more failed pods.
          impact: Scheduled automation such as backup, sync, or maintenance may not have
            completed.
          investigate: Check the Job, related pods, and their logs.
          runbook_url: https://kubernetes.io/docs/concepts/workloads/controllers/job/
          summary: Job {{ $labels.namespace }}/{{ $labels.job_name }} has failed
        expr: kube_job_status_failed{namespace!~"kube-system"} > 0 unless on (namespace,
          job_name) kube_job_status_succeeded{namespace!~"kube-system"} > 0
        for: 15m
        labels:
          category: kubernetes
          scope: job
          severity: warning
    - name: cluster-critical-runtime
      rules:
      - alert: KubernetesContainerCrashLooping
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}&var-pod={{ $labels.pod }}
          description: Container {{ $labels.container }} in pod {{ $labels.namespace }}/{{
            $labels.pod }} has been in CrashLoopBackOff for more than 10 minutes.
          impact: Application availability is degraded or unavailable.
          investigate: Inspect pod logs, restart history, events, and recent config or
            secret changes.
          runbook_url: https://kubernetes.io/docs/tasks/debug/debug-application/debug-pods/
          summary: Pod {{ $labels.namespace }}/{{ $labels.pod }} is crash looping
        expr: max by (cluster, namespace, pod, container) (kube_pod_container_status_waiting_reason{reason="CrashLoopBackOff",namespace!~"kube-system"})
          > 0
        for: 10m
        labels:
          category: kubernetes
          scope: pod
          severity: critical
      - alert: KubernetesPodHighRestarts
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace={{
            $labels.namespace }}&var-pod={{ $labels.pod }}
          description: Container {{ $labels.container }} in pod {{ $labels.namespace }}/{{
            $labels.pod }} restarted more than 5 times in 30 minutes.
          impact: Application may be unstable even if it still appears up.
          investigate: Check pod logs, readiness/liveness probes, resource pressure, and
            dependency failures.
          runbook_url: https://kubernetes.io/docs/tasks/debug/debug-application/debug-pods/
          summary: Pod {{ $labels.namespace }}/{{ $labels.pod }} is restarting frequently
        expr: increase(kube_pod_container_status_restarts_total{namespace!~"kube-system",container!="POD"}[30m])
          > 5
        for: 10m
        labels:
          category: kubernetes
          scope: pod
          severity: warning
      - alert: NodeFilesystemAlmostFull
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/dUMN5x0mk/kubernetes-nodes?var-server={{
            $labels.instance }}
          description: Available space on {{ $labels.instance }} root filesystem is below
            10% for more than 15 minutes.
          impact: Pods, logs, and node services may fail unexpectedly.
          investigate: Check disk usage, container logs, image cache, and Longhorn or
            application storage growth.
          runbook_url: https://kubernetes.io/docs/concepts/cluster-administration/logging/
          summary: Root filesystem on node {{ $labels.instance }} is almost full
        expr: (node_filesystem_avail_bytes{mountpoint="/",fstype!~"tmpfs|fuse.lxcfs|overlay"}
          / node_filesystem_size_bytes{mountpoint="/",fstype!~"tmpfs|fuse.lxcfs|overlay"})
          < 0.10 and node_filesystem_readonly{mountpoint="/",fstype!~"tmpfs|fuse.lxcfs|overlay"}
          == 0
        for: 15m
        labels:
          category: node
          scope: node
          severity: critical
    - name: cluster-critical-platform
      rules:
      - alert: CiliumDaemonSetUnavailable
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/0a93a907-30c0-4f93-86f2-a34f3b4ed0d7/cilium-metrics?var-node={{
            $labels.node }}
          description: Cilium pod {{ $labels.pod }} on node {{ $labels.node }} has been
            NotReady for more than 10 minutes.
          impact: Cluster networking, service routing, and policy enforcement may be degraded.
          investigate: Check Cilium pod health, node conditions, recent upgrades, and
            Hubble/Cilium metrics.
          runbook_url: https://docs.cilium.io/en/stable/operations/troubleshooting/
          summary: Cilium pod {{ $labels.pod }} on node {{ $labels.node }} is unavailable
        expr: max by (cluster, namespace, pod, node) ((kube_pod_status_ready{namespace="kube-system",condition="true",pod=~"cilium-.*"}
          == 0) * on (namespace, pod) group_left(node) kube_pod_info{namespace="kube-system",pod=~"cilium-.*"})
          > 0
        for: 10m
        labels:
          category: networking
          scope: platform
          severity: critical
      - alert: HubbleRelayUnavailable
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/cZB7u29Mz/cluster-requests-and-limits-specific?var-namespace=kube-system&var-pod=hubble-relay.*
          description: The Hubble Relay deployment in kube-system has no available replicas
            for more than 10 minutes.
          impact: Network flow visibility and troubleshooting are degraded.
          investigate: Check hubble-relay pods, service, and cilium-agent connectivity.
          runbook_url: https://docs.cilium.io/en/stable/observability/hubble/setup/
          summary: Hubble Relay is unavailable
        expr: kube_deployment_status_replicas_available{namespace="kube-system",deployment="hubble-relay"}
          < 1
        for: 10m
        labels:
          category: networking
          scope: platform
          severity: warning
      - alert: ExternalMonitoringTargetDown
        annotations:
          dashboard_url: https://grafana.cluster.home.tlta.online/d/dUMN5x0mk/kubernetes-nodes?var-server={{
            $labels.instance }}
          description: Alloy cannot scrape external target {{ $labels.job }} at {{ $labels.instance
            }} for more than 10 minutes.
          impact: Visibility into LAN infrastructure is incomplete.
          investigate: Check host reachability, exporter health, LAN routing, and firewall
            rules.
          runbook_url: https://prometheus.io/docs/prometheus/latest/querying/basics/
          summary: External monitoring target {{ $labels.job }} is down
        expr: up{job=~"rasp4-node|minidesk-host|maindesk-host|openwrt-router|haproxy-router"}
          == 0
        for: 10m
        labels:
          category: external-dependency
          scope: monitoring
          severity: warning

---
# Source: alerting-stack/templates/alertmanager-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: alerting-stack-alertmanager
  namespace: monitoring
  labels:
    app: alertmanager
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "50Mi"
---
# Source: alerting-stack/templates/clusterrolebinding.yaml
kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: alerting-stack-server-clusterrolebinding
  namespace: monitoring
  labels:
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
subjects:
  - kind: ServiceAccount
    name: alerting-stack
    namespace: monitoring
roleRef:
  kind: ClusterRole
  name: alerting-stack-server-clusterrole
  apiGroup: rbac.authorization.k8s.io
---
# Source: alerting-stack/templates/alertmanager-service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: monitoring
  labels: 
    app: alertmanager
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
  name: alerting-stack-alertmanager
spec:
  type: ClusterIP
  ports:
    - name: web
      port: 9093
      targetPort: web
      protocol: TCP
  selector: 
    app: alertmanager
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack

---
# Source: alerting-stack/templates/server-service.yaml
apiVersion: v1
kind: Service
metadata:
  namespace: monitoring
  labels: 
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
  name: alerting-stack-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8880
      targetPort: http
      protocol: TCP
  selector: 
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack

---
# Source: alerting-stack/templates/alertmanager-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alerting-stack-alertmanager
  namespace: monitoring
  labels: 
    app: alertmanager
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  selector:
    matchLabels: 
      app: alertmanager
      app.kubernetes.io/name: alerting-stack
      app.kubernetes.io/instance: alerting-stack
  strategy:
    # Must be "Recreate" when we have a persistent volume
    type: Recreate
  template:
    metadata:
      labels: 
        app: alertmanager
        app.kubernetes.io/name: alerting-stack
        app.kubernetes.io/instance: alerting-stack
        helm.sh/chart: alerting-stack-0.10.0
        app.kubernetes.io/managed-by: Helm
      annotations: 
        checksum/config: 01ba4719c80b6fe911b091a7c05124b64eeece964e09c058ef8f9805daca546b
    spec:
      serviceAccountName: alerting-stack
      automountServiceAccountToken: true
      containers:
        - name: alertmanager
          image: prom/alertmanager:v0.25.0
          args:
            - --config.file=/config/alertmanager.yaml
            - --storage.path=/data
            - --data.retention=120h
            - --web.listen-address=0.0.0.0:9093
            - --cluster.advertise-address=$(POD_IP):6783
            
            
          ports:
            - name: web
              containerPort: 9093
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
          readinessProbe: 
            httpGet:
              path: /-/ready
              port: web
              scheme: HTTP
          livenessProbe: 
            httpGet:
              path: /-/healthy
              port: web
              scheme: HTTP
          startupProbe: 
            httpGet:
              path: /-/ready
              port: web
              scheme: HTTP
          volumeMounts:
            - name: storage
              mountPath: /data
            - name: config
              mountPath: /config
              readOnly: true
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: alerting-stack-alertmanager
        - name: config
          configMap:
            name: alertmanager-config
---
# Source: alerting-stack/templates/server-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alerting-stack-server
  namespace: monitoring
  labels: 
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
spec:
  minReadySeconds: 0
  replicas: 1
  selector:
    matchLabels: 
      app: server
      app.kubernetes.io/name: alerting-stack
      app.kubernetes.io/instance: alerting-stack
  strategy: 
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels: 
        app: server
        app.kubernetes.io/name: alerting-stack
        app.kubernetes.io/instance: alerting-stack
        helm.sh/chart: alerting-stack-0.10.0
        app.kubernetes.io/managed-by: Helm
      annotations: 
        checksum/config: b52f926009fdaed9cfd6d2b0838cc3ec245151567fe222bd28d2d575b92c68b1
    spec:
      serviceAccountName: alerting-stack
      securityContext: 
        {}
      automountServiceAccountToken: true
      containers:
        - name: vmalert
          securityContext: 
            {}
          image: victoriametrics/vmalert:v1.102.1
          args:
            - -rule=/config/alert-rules.yaml
            - -datasource.url=http://victoria-metrics-single-server.monitoring.svc.cluster.local:8428
            - -notifier.url=http://alerting-stack-alertmanager:9093
            - -envflag.enable=true
            - -envflag.prefix=VM_
            - -loggerFormat=json
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8880
          readinessProbe: 
            failureThreshold: 3
            httpGet:
              path: /health
              port: http
              scheme: HTTP
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 5
          livenessProbe: 
            failureThreshold: 3
            initialDelaySeconds: 5
            periodSeconds: 15
            tcpSocket:
              port: http
            timeoutSeconds: 5
          volumeMounts:
            - name: alerts-config
              mountPath: /config
            
      volumes:
        - name: alerts-config
          configMap:
            name: alerting-stack-server-alert-rules-config
        
---
# Source: alerting-stack/templates/service-monitor.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels: 
    app: server
    app.kubernetes.io/name: alerting-stack
    app.kubernetes.io/instance: alerting-stack
    helm.sh/chart: alerting-stack-0.10.0
    app.kubernetes.io/managed-by: Helm
  name: alerting-stack-server
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - monitoring
  selector:
    matchLabels: 
      app: server
      app.kubernetes.io/name: alerting-stack
      app.kubernetes.io/instance: alerting-stack
  endpoints:
    - port: http
